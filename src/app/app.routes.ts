import { Routes } from '@angular/router';
import { SurpriseMainPageComponent } from './features/pages/surprise-main-page/surprise-main-page.component';

export const routes: Routes = [
    { path: 'surprise', component: SurpriseMainPageComponent }
];
